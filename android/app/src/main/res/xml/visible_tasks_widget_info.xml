<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="200dp"
    android:minHeight="100dp"
    android:targetCellWidth="3"
    android:targetCellHeight="2"
    android:updatePeriodMillis="86400000"
    android:previewImage="@mipmap/ic_launcher"
    android:initialLayout="@layout/visible_tasks_widget_layout"
    android:description="@string/widget_description"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen">
    <!-- For lock screen, you might try adding 'keyguard' to widgetCategory,
         but support varies: android:widgetCategory="home_screen|keyguard"
         Modern Android lock screens are more restrictive with custom widgets. -->
</appwidget-provider> 